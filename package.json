{
  "name": "support-desk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "internal/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace @support-desk/api",
    "dev:web": "npm run dev --workspace @support-desk/web",
    "build": "tsc -b && npm run build --workspace @support-desk/web",
    "preview": "npm run preview --workspace @support-desk/web",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "lint:strict": "HARNESS_STRICT_LINT=1 eslint .",
    "lint:boundaries": "depcruise apps packages --config .dependency-cruiser.cjs",
    "lint:duplication": "node internal/duplication-check/src/cli.js",
    "lint:classes": "node internal/class-resolution/src/cli.js",
    "test": "vitest run",
    "test:mutation": "stryker run",
    "test:mutation:fast": "stryker run stryker.fast.config.json",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@eslint/markdown": "^8.0.3",
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "@types/node": "^24.13.3",
    "@vitest/coverage-v8": "^4.1.11",
    "axe-core": "^4.13.0",
    "concurrently": "^10.0.5",
    "dependency-cruiser": "^18.2.0",
    "eslint": "^10.9.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.4",
    "globals": "^17.11.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  }
}
